buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:2.1.3'
    }
}

allprojects {
    repositories {
        maven { url 'libs' }
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
